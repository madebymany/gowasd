packages:
  branch_codenames:
    master: unstable
    production: stable

jobs:
  wasd:
    builder: "golang"
    base: "quay.io/madebymany/build-bases:trusty"
    environment:
      GO_PACKAGE_DIR: wasd
    packages:
      - type: debian
        name: wasd
        version: 0.0.1
        target: install
        repos:
          - mxm-public
        activates:
          - wasd-upgrade
